services:
  server:
    build: ./server
    ports:
      - "3001:3001"
    environment:
      CORS_ORIGIN: http://localhost:3000

  client:
    build:
      context: ./client
      args:
        VITE_API_URL: http://localhost:3001
    ports:
      - "3000:3000"
    depends_on:
      - server
